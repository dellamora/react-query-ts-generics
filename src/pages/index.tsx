import { type NextPage } from "next";
import Footer from "../modules/footer";
import Hero from "../modules/hero";

const Home: NextPage = () => {
  return (
<div>
  <Hero/>
</div>
  );
};

export default Home;
